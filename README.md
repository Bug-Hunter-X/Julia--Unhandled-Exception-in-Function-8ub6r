# Julia Unhandled Exception Example

This repository demonstrates a simple example of an unhandled exception in Julia. The `bug.jl` file contains a function that throws an error when a negative number is passed as an argument.  The `bugSolution.jl` file shows how to improve the error handling.